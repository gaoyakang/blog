const state = {
  articleMenuSource: []
}

const mutations = {}

const actions = {
  setArticleMenuSource (store, articleMenuSource) {
    store.state.articleMenuSource = articleMenuSource
  }
}

export default {
  state,
  mutations,
  actions
}
